
export enum Type {
    Zero  = 0,
    One   = 1,
    Two   = 2,
    Three = 3,
    Four  = 4,
    Five  = 5,
    Six   = 6,
    Seven = 7,
    Eight = 8,
    Mine  = 9
}

export const toMineCount = (content: Type): number => (
    content
)

export const of = (mineCount: number): Type => (
    mineCount
)
